<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="gachon.project.withus.controller.dao.IBoardSelectResponseDao">
    <select id="list" resultType="gachon.project.withus.controller.dto.BoardSelectResponseDTO">
        select * from withus_test
    </select>

</mapper>