<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="gachon.project.withus.controller.dao.IBoardDao">
    <select id="list" resultType="gachon.project.withus.controller.dto.BoardDTO">
        select board_id, board_writer, board_title from board where board_deleteYN = 1
    </select>

    <select id="view" resultType="gachon.project.withus.controller.dto.BoardDTO">
        select * from board where board_id = #{param1} AND board_deleteYN = 1;
    </select>

    <insert id="write">
        insert into board(board_writer, board_title, board_content, board_category)
        values(#{param1}, #{param2}, #{param3}, #{param4})
    </insert>

    <delete id="delete">
        update board set board_deleteYN = 0 where board_id = #{param1}
    </delete>

</mapper>